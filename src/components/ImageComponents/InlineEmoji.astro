---
import { Image } from "astro:assets";

interface Props {
  emoji: ImageMetadata;
}

const { emoji } = Astro.props;
---

<Image src={emoji} alt={emoji.src.split("/").at(-1) || "mutant emoji"} class="max-w-8 pb-2 inline-block m-auto" />
