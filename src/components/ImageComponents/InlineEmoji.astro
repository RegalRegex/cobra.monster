---
import { Image } from "astro:assets";

interface Props {
  emoji: ImageMetadata;
  styles?: string;
}

const { emoji, styles = "" } = Astro.props;
---

<Image src={emoji} alt={emoji.src.split("/").at(-1) || "mutant emoji"} class:list={[`max-w-8 inline-block m-auto`, `${styles}`]} />
